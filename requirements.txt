# ---- Agent orchestration -------------------------------------------------
langgraph>=0.2.60,<0.4
langchain-core>=0.3.25,<0.4

# ---- LLM providers (any ONE is enough to run; see docs/SETUP.md) ---------
langchain-google-genai>=2.0.7        # Gemini  (primary)
langchain-openai>=0.2.14             # OpenRouter (OpenAI-compatible endpoint)
langchain-ollama>=0.2.2              # Local Ollama (air-gapped fallback)

# ---- Warehouse adapters --------------------------------------------------
google-cloud-bigquery>=3.27.0        # production warehouse
google-cloud-bigquery-storage>=2.27.0
db-dtypes>=1.3.1                     # BQ -> pandas type bridge
duckdb>=1.1.3                        # offline mirror of thelook_ecommerce
pandas>=2.0.3
numpy>=1.24.4

# ---- SQL safety ----------------------------------------------------------
sqlglot>=25.34.0                     # parse/validate/rewrite SQL as an AST

# ---- CLI / config --------------------------------------------------------
typer>=0.15.1
rich>=13.9.4
pydantic>=2.10.4
pydantic-settings>=2.7.0
PyYAML>=6.0.2
python-dotenv>=1.0.1
tenacity>=9.0.0

# ---- QA ------------------------------------------------------------------
pytest>=8.3.4
